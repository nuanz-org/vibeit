{
  "name": "@repo/api",
  "private": true,
  "scripts": {
    "dev": "uv run fastapi dev src/main.py --port 8000",
    "start": "uv run fastapi run src/main.py --host 0.0.0.0 --port 8000",
    "db:migrate": "uv run python scripts/migrate.py"
  }
}
