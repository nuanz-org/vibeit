/**
 * Hand-authored runtime fixtures (M2a4+).
 * Loaded inside the sandboxed frame — not executed in the parent app heap.
 */

export {
  SOCIAL_FRAME_ASPECT,
  SOCIAL_FRAME_TOOL_ID,
  createSocialFrameTool,
  socialFrameCreative,
} from "./social-frame";
